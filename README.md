# mvp-rodovias
mvp
# Conecta sua pasta local ao servidor do GitHub
git remote add origin https://github.com/SEU_USUARIO/mvp-rodovias.git

# Define a ramificação principal como 'main'
git branch -M main

# Envia os arquivos para a nuvem
git push -u origin main
